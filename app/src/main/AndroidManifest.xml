<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.gb.thegithubclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <application

        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheGitHubClient">
        <service
            android:name=".service.CustomIntentService"
            android:exported="true">
        <intent-filter>
            <action android:name="ru.gb.thegithubclient.service.CustomIntentService"/>
        </intent-filter>
    </service>


        <activity
            android:name=".ui.users.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.users.UserDetailActivity"
            android:label="@string/app_name" />

    </application>

</manifest>